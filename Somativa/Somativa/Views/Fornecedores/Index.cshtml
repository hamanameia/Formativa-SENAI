@model IEnumerable<Somativa.Models.Fornecedor>

@{
    ViewData["Title"] = "Fornecedores";
}

<h1 class="textocima">Fornecedores</h1>

<p>
    <a asp-action="Create" class="btn adidi rounded-pill">Adicionar</a>
</p>
<table class="table">
    <thead class="py-5">
        <tr class="row">
            <th class="border-0 d-none d-sm-table-cell col-1"></th>
            <th class="col-6">
                @Html.DisplayNameFor(model => model.Nome)
            </th>
            <th class="col-6 col-sm-5">
                @Html.DisplayNameFor(model => model.Cnpj)
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr class="row">
                <td class="border-0 col-1 d-none d-sm-table-cell">
                    <a asp-action="Edit" asp-route-id="@item.FornecedorId" class="text-decoration-none">
                        <svg bootstrap-icon="PencilSquare" width="24" height="24" class="text-dark me-2 my-2"></svg>
                    </a>
                    <a data-bs-toggle="modal" data-bs-target="#ModalTrash-@item.FornecedorId">
                        <svg bootstrap-icon="Trash" width="24" height="24" class="text-dark me-2 my-2"></svg>
                    </a>
                </td>
                <td class="col-6">
                    <div class="my-2">@Html.DisplayFor(modelItem => item.Nome)</div>
                </td>
                <td class="col-6 col-sm-5">
                    <div class="my-2">@Html.DisplayFor(modelItem => item.Cnpj)</div>
                </td>
            </tr>
            <tr class="d-sm-none w-100 d-flex ">
                <td class="col-6 justify-content-center d-flex">
                    <a asp-action="Edit" asp-route-id="@item.FornecedorId">
                        <svg bootstrap-icon="PencilSquare" width="24" height="24" class="text-dark me-2 my-2"></svg>
                    </a>
                </td>
                <td class="col-6 pe-0 justify-content-center d-flex">
                    <a data-bs-toggle="modal" data-bs-target="#ModalTrash-@item.FornecedorId">
                        <svg bootstrap-icon="Trash" width="24" height="24" class="text-dark me-2 my-2"></svg>
                    </a>
                </td>
            </tr>
            <div class="modal fade" id="ModalTrash-@item.FornecedorId" data-bs-keyboard="false" aria-labelledby="staticBackdropLabel" tabindex="-1" aria-hidden="true">
                <div class="modal-dialog modal-dialog-centered">
                    <div class="modal-content">
                        <div class="modal-header justify-content-center border-0">
                            <h5 class="modal-title text-center volt px-5 py-1 fs">Apagar</h5>
                        </div>
                        <div class="modal-body text-center border-0">
                            <p class="text-dark fs-3 fst-normal">Tem certeza que deseja apagar os dados do @Html.DisplayFor(modelItem => item.Nome)?</p>
                        </div>
                        <div class="modal-footer d-flex justify-content-center border-0">
                            <form asp-action="Delete" asp-route-id="@item.FornecedorId" class="me-5">
                                <input type="hidden" asp-for="@item.FornecedorId" />
                                <button type="submit" class="btn apag text-white">Deletar</button>
                            </form>
                            <button data-bs-dismiss="modal" class="btn volt2 text-white">Cancelar</button>
                        </div>
                    </div>
                </div>
            </div>
        }
    </tbody>
</table>
